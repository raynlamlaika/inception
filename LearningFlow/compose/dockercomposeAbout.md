docker compose is a tool builded for multiple docker services 'yes it can be used for only one'
it manage the networking of multiple services and make the life easy when it come to the big projects 

to set up on it database the auth service ai service ... or any other service instead of runing every one by
it own

first this tool most set up for it the blue print of the working flow with docker-compose.yaml file most be writing in yaml format


jahd jahd